# =====================================================
# FASTFOOD DELIVERY - ALERTMANAGER CONFIGURATION
# =====================================================
# Copy this file to .env and fill in your credentials
#
# IMPORTANT: Never commit .env file to version control!
# =====================================================

# =====================================================
# SLACK CONFIGURATION
# =====================================================
# 1. Go to https://api.slack.com/apps
# 2. Create New App → From Scratch
# 3. Go to "Incoming Webhooks" → Activate
# 4. Click "Add New Webhook to Workspace"
# 5. Select channel (e.g., #fastfood-alerts)
# 6. Copy the Webhook URL below

SLACK_WEBHOOK_URL=your-slack-webhook-url-here

# =====================================================
# EMAIL CONFIGURATION (Gmail SMTP)
# =====================================================
# 1. Enable 2-Factor Authentication on your Gmail
# 2. Go to https://myaccount.google.com/apppasswords
# 3. Create new App Password (Select "Mail" and your device)
# 4. Copy the 16-character password (without spaces)

# Your Gmail address
SMTP_USERNAME=your-email@gmail.com

# Gmail App Password (NOT your regular Gmail password!)
# Format: xxxx xxxx xxxx xxxx (16 characters, spaces removed)
SMTP_PASSWORD=yourapppassword

# Email address to receive alerts
ALERT_EMAIL_TO=admin@your-company.com

# =====================================================
# OPTIONAL: Multiple recipients
# =====================================================
# For multiple email recipients, separate with comma:
# ALERT_EMAIL_TO=admin@company.com,devops@company.com,oncall@company.com

# =====================================================
# VERIFICATION STEPS
# =====================================================
# After configuration:
# 1. Run: cd monitoring && docker-compose -f docker-compose.monitoring.yml up -d alertmanager
# 2. Check logs: docker logs alertmanager
# 3. Visit http://localhost:9093 to verify Alertmanager is running
# 4. Test alert in Prometheus Alerts tab or trigger manually
